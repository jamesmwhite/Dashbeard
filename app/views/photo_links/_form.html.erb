<%= form_for(@photo_link) do |f| %>
  <% if @photo_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_link.errors.count, "error") %> prohibited this photo_link from being saved:</h2>

      <ul>
      <% @photo_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :hidden %><br>
    <%= f.check_box :hidden %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
